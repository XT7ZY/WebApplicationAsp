@model IEnumerable<Parent>
@{
    ViewData["Title"] = "Список учителей";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Список родителей</h1>

<div class="conrainer p-3 "> 
    <div class="row pt-4">
        <div class=" col-6">
        <a class="btn btn-primary" asp-controller="Parent" asp-action="Create"> 
            <i class="bi bi-plus-circle"></i> &nbsp; Создать нового родителя
        </a>
        </div>
        <div class=" col-6 text-end">
        </div>
    </div>
    &nbsp;
<table class="table table-bordered table-striped" style="width: 100%">
    <thead>
        <tr>
            <th> 
                Имя
            </th>
            <th> 
                Фаилия
            </th>
            <th> 
                Отчество
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in @Model)
        {
            <tr>
                <td width="20%"> 
                    @obj.Name
                </td>
                <td width="30">
                    @obj.LastName
                </td>
                <td width="30%"> 
                    @obj.SurName
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Role" asp-action="Admin" class="w-100 btn btn-secondary btn-lg">Назад к администрированию</a>
</div>

